package com.tripath.data.model

/**
 * Represents the muscle group focus for strength training workouts.
 * Only applicable when WorkoutType is STRENGTH.
 */
enum class StrengthFocus {
    FULL_BODY,
    UPPER,
    LOWER,
    HEAVY,
    STABILITY
}

